**Prompt used to generate the code:**

"Take on the role of an experienced html and javascript developer.
I want you to develop a basic bootstrap styled web with a stopwatch functionality. Write the code using best practises, with indented code, comments and so on...
The web should contain:
Two squares, one with an arrow icon pointing up and the other one with an arrow icon pointing down (this is the 'home page' take a look at first screenshot).
The square that has the arrow up should also have a text saying 'StopWatch' under the arrow.
The square that has the arrow down should also have a text saying 'Countdown' under the arrow.
When the 'StopWatch' square is clicked a transition should show a timer interface like in the second screenshot attached. The timer should be completely functional showing hours, minutes, seconds and miliseconds. The timer can be started, paused or resumed (then the button text will change to paused), also it will be possible to clear/restart the timer. This functionality should work in realtime.
When the 'Countdown' square is clicked a transition should show a countdown interface similar to the third screenshot attached. The countdown should be completely functional, showing hours, minutes, seconds and miliseconds. The countdown can be setted (numbers should appear to click and set from seconds to hours when a number is clicked) and cleared. When the count reaches zero, a soft alarm will sound for 3 seconds.
Also in both functionalities it should appear an arrow to go to 'main page' under the main div where the functionality is located.
This must be encapsulated inside a div. align everything to the center, so it should appear at the middle of the screen.
I want this functionality in three files:
index.html only html code, css styles, javascript referenced file and bootstrap initialization.
script.js where the javascript code related to every single functionality is declared.
prompt.md this file will contain the prompt used to achieve this functionality and also should have the AI assistant used."

**AI Assistant Used:** ChatGPT (GPT-4, June 2025)
